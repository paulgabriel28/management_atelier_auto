#pragma once

#include <string>

#include <ctime>
#include <algorithm>

#include "../enum.h"
#include "../utile/functions.h"

using namespace std;

long long UnixToZile(const long long &);
long long ZileToUnix(const int &);
