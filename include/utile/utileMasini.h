#pragma once

#include <string>
#include <ctime>
#include <algorithm>

#include "../enum.h"

using namespace std;

unsigned int getVechime(const unsigned int &);

typeMasini stringToTypeMasina(const string &);